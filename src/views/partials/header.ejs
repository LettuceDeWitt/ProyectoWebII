<header class="site-header">
  <h1><a href="/">Cine de Terror</a></h1>
  <div class="actions">
    <% if (user) { %>
      <a class="btn" href="/admin">Admin</a>
      <a class="btn" href="/logout">Logout (<%= user.username %>)</a>
    <% } else { %>
      <a class="btn" href="/login">Admin</a>
    <% } %>
  </div>
</header>
